setenv THOR_HOME ${HOME}/thor

setenv PATH ${PATH}:${THOR_HOME}/bin
setenv LD_LIBRARY_PATH ${THOR_HOME}/lib

setenv LANG en_US

alias bin "cd ${THOR_HOME}/bin"
alias lib "cd ${THOR_HOME}/lib"
alias log "cd ${THOR_HOME}/log"
alias src "cd ${THOR_HOME}/src"

